{% extends 'base.html.twig' %}

{% block title %}Campagne{% endblock %}

{% block body %}
    <h1>Campagne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campagne.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ campagne.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ campagne.description }}</td>
            </tr>
        </tbody>
    </table>

    {% include 'front/campagne/promesseCampagne.html.twig' %}

    <a href="{{ path('app_acceuil') }}">Retourner à l'acceuil</a>
    {% if app.user %}
    <a href="{{ path('app_campagne_edit', {'id': campagne.id}) }}">edit</a>
    {% endif %}
    {{ include('front/campagne/_delete_form.html.twig') }}
{% endblock %}
